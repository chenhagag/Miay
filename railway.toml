[build]
builder = "dockerfile"

[deploy]
startCommand = "npx prisma migrate deploy --schema=server/prisma/schema.prisma && node server/src/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
